AGENDA

// Step thru this:
// $.fn.toggleSprite = function (firstPosition, secondPosition) {
//     return $(this).css('background-position',    function(idx, sp){
//         return sp = sp == firstPosition ? secondPosition : firstPosition;
//     });
// };

// JS Code review - anything wonky?

// Increment/Decrement in Js (fiddles) 

// Abstract the sprite positions? Overkill?

// Snippet/Library Tools

// Research plugins in jquery site

// SASS



// Cache as much as you can 
// 	use $ as a naming convention

// Use .bind as a general best practice instead of .mouseup .click .mousedown


// $('.icon.check-box.unchecked').bind('click',function(e){
// 	e.preventDefault();
// 	$(this).toggleSprite('-1px 50%','-21px 50%');
// 	$('.completed-item').prepend($(this).closest('li'));
// });

refactor the to-do JS

